// gallery viewer

(function (d) {
  'use strict';

  window.addEventListener('load', function () {
    var container = d.getElementById('gallery');
    container.classList.add('panel');
    console.log(container)
  });
})(document);
